ALTER SESSION SET CONTAINER=pdb;
CREATE USER usr IDENTIFIED BY pwd;
GRANT CONNECT, RESOURCE, EXP_FULL_DATABASE, IMP_FULL_DATABASE TO usr;
ALTER USER usr QUOTA UNLIMITED ON USERS;

CREATE DIRECTORY dump_dir as '/home/oracle/dump';
GRANT READ, WRITE ON DIRECTORY dump_dir to usr;

EXIT;